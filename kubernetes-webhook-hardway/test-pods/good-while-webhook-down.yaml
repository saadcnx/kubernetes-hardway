apiVersion: v1
kind: Pod
metadata:
  name: good-while-webhook-down
  namespace: production
spec:
  containers:
  - name: nginx
    image: nginx:1.25-alpine
    resources:
      limits:
        cpu: "100m"
apiVersion: v1
kind: Pod
metadata:
  name: good-nginx
  namespace: production
spec:
  containers:
  - name: nginx
    image: nginx:1.25-alpine
    resources:
      limits:
        cpu: "100m"
        memory: "128Mi"
      requests:
        cpu: "50m"
        memory: "64Mi"